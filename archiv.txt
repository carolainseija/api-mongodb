//route

//put que  modifica una cancion revisar
  .put(async(req, res) => {
    try {
      const name = req.params.name;
      const modificacion = req.body;
      const modificarCanciones = await controller.modificaUnaCancion(name, modificacion);
      if (modificaUnaCancion){
        res.status(404).send("algo salio mal");
      } else {
        res.status(201).send("cancion modificada");
      }
    } catch (e) {
      throw e;
    }
  })

//cont
const modificarCanciones = async(name, modificacion) => {
    return await baseDeDatos.modificarUnaCancion(name, modificacion);
}


//model
const modificarUnaCancion = async(name, modificacion) => {
    const cancionModificada = await Cancion.findOneAndUpdate({name: name}, modificacion);
    cancionModificada.save();
}
